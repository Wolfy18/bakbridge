import React, { PropsWithChildren } from 'react';
interface Props {
    enableHeader?: boolean;
    enableFooter?: boolean;
}
declare const SimpleLayout: React.FC<Props & PropsWithChildren>;
export default SimpleLayout;
